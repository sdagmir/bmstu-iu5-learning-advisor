"""Системные промпты и определения функций для LLM-оркестратора.

Три функции доступны LLM через function calling:
1. get_recommendations — получить рекомендации по текущему профилю
2. simulate_changes — смоделировать what-if-пересчёт без записи в БД
3. search_knowledge — семантический поиск по базе знаний (RAG)
"""

from __future__ import annotations

from typing import Any

SYSTEM_PROMPT = """\
Ты — персональный навигатор по профессиональному развитию студента кафедры ИУ5 \
МГТУ им. Н.Э. Баумана. Помогаешь формировать индивидуальную образовательную траекторию.

Твоя роль — ПЕРЕВОДЧИК между студентом и экспертной системой. \
Ты НЕ принимаешь решений о рекомендациях. Все рекомендации формирует экспертная система (ЭС).

Что уже знает ЭС (вызывается одним get_recommendations() — БЕЗ переспрашиваний):
- какие программы цифровой кафедры взять (категория ck_programs)
- по каким дисциплинам учебного плана в текущем семестре есть курсовая \
и какую тему выбрать под цель (категория coursework)
- какой фокус взять в дисциплинах учебного плана (категория focus)
- стоит ли идти в Технопарк и какое направление (категория technopark)
- предупреждения о слабых базах (категория warning)
- стратегические советы (категория strategy)

Правила работы:
- Отвечай на русском языке.
- Используй function calling — НЕ переспрашивай студента, если ответ можно получить из тулы. \
Уточняй только если запрос неоднозначен И ни один тул не поможет.
- Любой вопрос типа «что мне выбрать / какая у меня курсовая / по какому предмету / какую тему / \
что взять / что посоветуешь / какая программа ЦК / нужен ли мне Технопарк / какой фокус» — \
СРАЗУ вызывай get_recommendations() и ищи нужную категорию в результате. \
Не задавай уточняющих вопросов.
- При уточняющих вопросах вида «а если...», «а без...», «а что если сменить \
цель/Технопарк/нагрузку» — вызови simulate_changes() с конкретным изменением. \
Это what-if-моделирование: профиль в БД НЕ меняется. Поэтому в ответе \
НИКОГДА не пиши «я изменил твою цель/Технопарк/нагрузку» — это вводит в \
заблуждение. Пиши «давай посмотрим как изменились бы рекомендации, если бы \
ты сменил цель на …» или «вот как ЭС пересчитала с этой гипотезой …». \
Для фактической смены параметра напомни студенту что нужно зайти в форму \
профиля и сохранить там.
- При вопросах о деталях курса/программы/Технопарка («расскажи про курс X», «что входит в Y», \
«какие требования к Z») — вызови search_knowledge() с конкретным запросом.
- При свободных вопросах (благодарность, мотивация) — отвечай самостоятельно.
- Объясняй рекомендации понятным языком, ссылаясь на обоснования из ЭС.
- При сравнении с предыдущим ответом — объясни, что изменилось и почему.
- Не выдумывай рекомендации — только то, что вернула ЭС или RAG.
- НЕ полагайся на ответы тулов из истории диалога. Профиль студента может смениться \
через форму, правила ЭС может править админ — устаревшие результаты создадут \
ложный ответ. На КАЖДЫЙ запрос про рекомендации/курсы/курсовые/Технопарк ЗАНОВО \
вызывай get_recommendations(), даже если кажется что уже знаешь ответ из истории. \
То же для search_knowledge() — заново на каждый информационный вопрос.

Особый случай — диалог про курсовые работы (категория coursework):
- В семестре может быть НЕСКОЛЬКО курсовых одновременно (например, в 4 семестре их две — \
по «Базы данных» и по «Системное программирование»). Если ЭС вернула несколько рекомендаций \
категории coursework — ОБЯЗАТЕЛЬНО упомяни ВСЕ дисциплины, а не только первую. Никогда не \
говори «у вас курсовая» в единственном числе, если их две — это вводит в заблуждение.
- ЭС возвращает по каждой курсовой обычно 3 пронумерованных варианта темы с подсказками \
«подходит, если интересует …».
- НЕ перечисляй все варианты сухим списком в первом ответе. Сначала перечисли по каким \
дисциплинам курсовые в этом семестре, затем спроси что студенту ближе \
(например: «по обеим есть по три направления — от прикладного проектирования до \
исследовательской работы. Что тебе интереснее: применить уже знакомый стек к \
продуктовой задаче, или копнуть в архитектуру/исследование?»). Можно сразу спросить про \
обе дисциплины одним вопросом, не разделяя.
- На ответ студента подсвети 1-2 варианта из ЭС по КАЖДОЙ курсовой, которые лучше \
подходят, и переформулируй их под его слова. При новом уточнении — снова сузь выбор.
- Если студент сразу просит «дай все темы» — давай по три из reasoning ЭС для каждой \
дисциплины, без выдумывания своих.\


Через диалог студент может СМОДЕЛИРОВАТЬ (what-if, без записи в БД) изменение:
- career_goal (карьерную цель)
- technopark_status (статус Технопарка)
- workload_pref (желаемую нагрузку)

Реальная смена этих параметров — только через форму профиля.
Параметры, которые нельзя моделировать через диалог (только через форму): \
семестр, пройденные курсы, оценки.\
"""

# Определения функций для function calling (OpenAI-совместимый формат)
TOOL_DEFINITIONS: list[dict[str, Any]] = [
    {
        "type": "function",
        "function": {
            "name": "get_recommendations",
            "description": (
                "Получить ВСЕ персональные рекомендации студента из ЭС: "
                "программы цифровой кафедры (ck_programs), темы курсовых работ по дисциплинам "
                "учебного плана (coursework), фокусы в дисциплинах (focus), стратегия Технопарка "
                "(technopark), предупреждения (warning), стратегические советы (strategy). "
                "Вызывай ВСЕГДА когда студент спрашивает «что мне выбрать», «по какому предмету», "
                "«какую тему», «какая программа», «нужен ли Технопарк», «что посоветуешь» — "
                "не переспрашивай студента, ответ уже здесь."
            ),
            "parameters": {
                "type": "object",
                "properties": {},
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "simulate_changes",
            "description": (
                "What-if-моделирование: пересчитать рекомендации, как если бы "
                "у студента были другие параметры профиля. ПРОФИЛЬ В БД НЕ "
                "МЕНЯЕТСЯ — это только гипотетический пересчёт для ответа в "
                "диалоге. Вызывай при уточняющих вопросах: «а если сменить "
                "цель на …», «а без Технопарка», «можно полегче». Передай "
                "только изменяемые поля. В ответе студенту НЕ говори «я "
                "изменил профиль» — говори «вот как изменились бы "
                "рекомендации, если бы …»."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "career_goal": {
                        "type": "string",
                        "enum": [
                            "ml",
                            "backend",
                            "frontend",
                            "cybersecurity",
                            "system",
                            "devops",
                            "mobile",
                            "gamedev",
                            "qa",
                            "analytics",
                            "undecided",
                        ],
                        "description": "Новая карьерная цель",
                    },
                    "technopark_status": {
                        "type": "string",
                        "enum": ["none", "backend", "frontend", "ml", "mobile"],
                        "description": "Новый статус Технопарка",
                    },
                    "workload_pref": {
                        "type": "string",
                        "enum": ["light", "normal", "intensive"],
                        "description": "Новая желаемая нагрузка",
                    },
                },
                "required": [],
            },
        },
    },
    {
        "type": "function",
        "function": {
            "name": "search_knowledge",
            "description": (
                "Семантический поиск по базе знаний: описания курсов, вакансии, "
                "информация о Технопарке. Вызывай при информационных вопросах."
            ),
            "parameters": {
                "type": "object",
                "properties": {
                    "query": {
                        "type": "string",
                        "description": "Поисковый запрос на русском языке",
                    },
                },
                "required": ["query"],
            },
        },
    },
]


def build_profile_context(profile_data: dict[str, Any]) -> str:
    """Построить текстовое описание профиля студента для контекста LLM."""
    goal_names = {
        "ml": "ML / Data Science",
        "backend": "Бэкенд-разработка",
        "frontend": "Фронтенд-разработка",
        "cybersecurity": "Кибербезопасность",
        "system": "Системное программирование",
        "devops": "DevOps",
        "mobile": "Мобильная разработка",
        "gamedev": "Геймдев",
        "qa": "QA / Тестирование",
        "analytics": "Аналитика данных",
        "undecided": "Не определился",
    }
    tp_names = {
        "none": "не участвует",
        "backend": "бэкенд",
        "frontend": "фронтенд",
        "ml": "ML",
        "mobile": "мобильная",
    }
    workload_names = {
        "light": "лёгкая",
        "normal": "нормальная",
        "intensive": "интенсивная",
    }

    goal = goal_names.get(profile_data.get("career_goal", ""), "неизвестно")
    semester = profile_data.get("semester", "?")
    tp = tp_names.get(profile_data.get("technopark_status", ""), "?")
    workload = workload_names.get(profile_data.get("workload_pref", ""), "?")

    parts = [
        f"Семестр: {semester}",
        f"Карьерная цель: {goal}",
        f"Технопарк: {tp}",
        f"Нагрузка: {workload}",
    ]

    if profile_data.get("weak_math"):
        parts.append("⚠ Слабая база по математике")
    if profile_data.get("weak_programming"):
        parts.append("⚠ Слабая база по программированию")

    return "Профиль студента:\n" + "\n".join(f"• {p}" for p in parts)


def format_recommendations_for_llm(recommendations: list[dict[str, Any]]) -> str:
    """Форматировать рекомендации ЭС в текст для передачи LLM."""
    if not recommendations:
        return "Экспертная система не выдала рекомендаций для данного профиля."

    lines = ["Результат экспертной системы:"]
    for rec in recommendations:
        priority = {"high": "🔴", "medium": "🟡", "low": "🟢"}.get(rec.get("priority", ""), "")
        lines.append(
            f"\n{priority} [{rec.get('category', '')}] {rec.get('title', '')}"
            f"\n   Обоснование: {rec.get('reasoning', '')}"
            f"\n   Правило: {rec.get('rule_id', '')}"
        )
        if rec.get("competency_gap"):
            lines.append(f"   Пробел: {rec['competency_gap']}")

    return "\n".join(lines)
